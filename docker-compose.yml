services:
  resonance:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./config:/app/data
    env_file:
      - .env
    environment:
      # Data directory for SQLite database (inside container)
      DATA_DIR: /app/data
      PORT: 3001
    restart: unless-stopped

